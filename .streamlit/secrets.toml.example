# Fichier de configuration des secrets pour Streamlit Cloud
# Copiez ce fichier en tant que secrets.toml et remplissez avec vos vraies valeurs
# ⚠️ NE JAMAIS COMMITER secrets.toml dans Git!

# ==================== BASE DE DONNÉES ====================
# Pour Streamlit Cloud, utilisez SQLite (par défaut) ou connectez une base externe

[database]
DB_TYPE = "sqlite"  # Options: "sqlite", "mysql", "postgresql"
DB_HOST = "localhost"
DB_NAME = "mairie_db"
DB_USER = "root"
DB_PASSWORD = ""
DB_PORT = "3306"

# ==================== EMAIL (OPTIONNEL) ====================
# Pour l'envoi d'emails automatiques

[email]
EMAIL_FROM = "votre_email@gmail.com"
EMAIL_PASSWORD = "votre_mot_de_passe_application"
EMAIL_TO = "destinataire@email.com"

# ==================== MQTT (OPTIONNEL) ====================
# Pour la communication IoT avec capteurs RFID

[mqtt]
MQTT_BROKER = "votre_broker.hivemq.cloud"
MQTT_PORT = "8883"
MQTT_USERNAME = "votre_username"
MQTT_PASSWORD = "votre_password"

# ==================== HEDERA HASHGRAPH (OPTIONNEL) ====================
# Pour les transactions blockchain

[hedera]
OPERATOR_ID = "0.0.XXXXXX"
OPERATOR_KEY = "302e020100300506032b6570..."
TOPIC_ID = "0.0.XXXXXX"
SUPPLIER_ACCOUNT_ID = "0.0.XXXXXX"

# ==================== INSTRUCTIONS ====================
# Sur Streamlit Cloud:
# 1. Allez dans App settings > Secrets
# 2. Copiez le contenu de ce fichier
# 3. Remplacez les valeurs par vos vraies credentials
# 4. Sauvegardez

# Pour accéder aux secrets dans le code:
# import streamlit as st
# db_type = st.secrets["database"]["DB_TYPE"]
# email = st.secrets["email"]["EMAIL_FROM"]
